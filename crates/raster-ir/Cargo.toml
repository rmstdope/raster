[package]
name = "raster-ir"
version = "0.1.0"
edition.workspace = true

[lints]
workspace = true

[dependencies]
raster-sema = { path = "../raster-sema" }
raster-syntax = { path = "../raster-syntax" }
