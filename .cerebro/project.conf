project_name   raster
default_branch main
audience_noun  NES developer
app_paths      ^(crates/|Cargo\.(toml|lock)$)
gate_fast      cargo test
gate_full      cargo fmt --check && cargo clippy -- -D warnings && cargo test
install_shell  rustup toolchain install stable && cargo fetch
agent_cli      claude
verification   none
